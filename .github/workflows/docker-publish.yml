name: docker-publish

on:
  push:
    branches: 
      - main
    
jobs:
  docker-publish:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v7

      - name: docker build
        run: docker build -t github-actions-practice:latest .
      
