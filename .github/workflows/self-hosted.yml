name: self-hosted runner

on:
  workflow_dispatch:

jobs:
  self-hosted-runner:
    runs-on: 
     - self-hosted
     - my-linux-runner

    steps:
      - name: hostname
        run: hostname

      - name: working directory
        run: pwd

      - name: file create 
        run: echo "hello fron self-hosted runner" > runner-test.txt

      - name: verify file
        run: ls -la runner-test.txt
