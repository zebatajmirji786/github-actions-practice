name: github-hosted

on:
  workflow_dispatch:
    
jobs:
  runner-test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
      - name: Show info
        run: |
          echo "the OS Name is : $RUNNER_OS"
          echo "$(hostname)" 
          echo "$(whoami)"

 
