name: github-hosted

on:
  workflow_dispatch:
    
jobs:
  runner-test:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    runs-on: ${{ matrix.os }}

    steps:
      - name: Show OS
        run: |
          echo"the OS Name is : $RUNNER_OS"
          echo "$(hostname)" 
          echo "$(whoami)"

 
